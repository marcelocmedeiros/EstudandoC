/**
   Operador	        Descrição	                  Exemplo:
    ==	        os valores são iguais	    (A == B) -> verdade.
    !=	        os valores são desiguais 	(A != B) -> verdade.
    >	    o valor A é maior do que o B 	(A > B) -> verdade.
    <		o valor A é menor do que o B    (A < B) -> verdade.
    >=	    o valor A é maior ou igual B	(A >= B) -> verdade.
    <=	    o valor A é menor ou igual B	(A <= B) -> verdade.
*/
